@model IEnumerable<LTQL_1721050441.Models.PVNSanPham441>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Sản Phẩm</h2>

<p>
    @Html.ActionLink("Thêm Mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NhaCungCap441s.TenNhaCungCap)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenSanPham)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NhaCungCap441s.TenNhaCungCap)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenSanPham)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaSanPham }) |
            @Html.ActionLink("Chi Tiết", "Details", new { id=item.MaSanPham }) |
            @Html.ActionLink("Xpas", "Delete", new { id=item.MaSanPham })
        </td>
    </tr>
}

</table>
